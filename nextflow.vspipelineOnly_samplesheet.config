params {
    // Use the normal pacbioGermline samplesheet and reconstruct old output paths.
    vspipelineOnly = true

    // Optional: run only one normalized testlist, e.g. SL_NGC_SJAELDNE.
    // vspipeline_testlist = 'SL_NGC_SJAELDNE'
}
